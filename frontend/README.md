# Healthcare Frontend

React + Vite frontend for the smart healthcare platform.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## Environment

Copy `.env.example` to `.env` and configure:

- `VITE_API_BASE_URL`

## Docker

The frontend is built with Vite and served through Nginx in the Docker image.
