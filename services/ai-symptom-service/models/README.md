`models/` at the service root is for Node-side data models if the service grows to use them.

The trained Python ML artifacts for symptom analysis are stored under:

- `ml-integration/models/`

Those files are generated by `ml-integration/train_model.py`.
